{
  "name": "@filbeam/workflows",
  "version": "1.0.0",
  "private": true,
  "description": "Shared workflows for FilBeam workers",
  "author": "Space Meridian <filbeam@meridian.space>",
  "type": "module",
  "main": "lib/transaction-monitor-workflow.js",
  "scripts": {
    "build:types": "wrangler types --env dev",
    "test": "vitest run"
  },
  "dependencies": {
    "viem": "^2.41.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.10.15"
  }
}
